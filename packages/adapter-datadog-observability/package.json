{
  "name": "@motiadev/adapter-datadog-observability",
  "description": "Datadog observability adapter for Motia framework, enabling APM tracing, logging, and metrics export to Datadog.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "biome check .",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@motiadev/core": "workspace:*",
    "dd-trace": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@motiadev/core": "^0.8.0"
  }
}
