import type { MotiaPluginContext } from '@motiadev/core'
import type { RedisClientType } from 'redis'

export const api = (_motia: MotiaPluginContext, _client: RedisClientType): void => {
  // API endpoints will be implemented in step 4
}
